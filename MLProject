name: mnist_training

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      hyperparams_config: {type: string, default: conf1}
    command: "python mnist_training.py hyperparameters={hyperparams_config}"